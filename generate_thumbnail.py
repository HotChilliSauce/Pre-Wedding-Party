from PIL import Image, ImageDraw, ImageFont\n\n# Create a blank image with dark background\nthumbnail_width = 1200\nthumbnail_height = 630\nimage = Image.new('RGB', (thumbnail_width, thumbnail_height), (0, 0, 0))\n\n# Initialize drawing context\ndraw = ImageDraw.Draw(image)\n\n# Load a font. You may need to specify the path to the .ttf font file.\nfont_path = 'path/to/elegant_font.ttf'  # Update this to a valid font path\nfont_size = 48\nfont = ImageFont.truetype(font_path, font_size)\n\n# Invitation details\ntext = "Join us for the Pre-Wedding Party\nPark JinBae & Oh YuGyung\nDate: 2026-02-15\nTime: 12:45 PM UTC"\ntext_width, text_height = draw.textsize(text, font=font)\n\n# Calculate text position to center it\ntext_x = (thumbnail_width - text_width) // 2\ntext_y = (thumbnail_height - text_height) // 2\n\n# Draw the text on the image\ndraw.text((text_x, text_y), text, fill=(255, 255, 255), font=font)\n\n# Save the thumbnail image\nimage.save('thumbnail.jpg')\n